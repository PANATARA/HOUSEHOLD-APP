module.exports = {
  semi: true, // ставить ;
  singleQuote: false, // использовать двойные кавычки
  trailingComma: "all", // запятые в объектах и массивах
  printWidth: 88, // длина строки
  tabWidth: 2, // пробелов в табе
  useTabs: false, // только пробелы
  bracketSpacing: true, // { foo: bar } вместо {foo: bar}
  arrowParens: "always", // (x) => вместо x =>
  endOfLine: "lf", // перенос строк LF (важно для Git)
};
